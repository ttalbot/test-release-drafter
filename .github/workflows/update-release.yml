on:
  push:
    branches:
      - main

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: release-drafter/release-drafter@v5
        with:
          name: 'fds@next'
          tag: 'fds@next'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}